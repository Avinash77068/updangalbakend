{
  "version": 2,
  "builds": [
    {
      "src": "api/server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["docs/**", "public/**"] }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/server.js" }
  ]
}
